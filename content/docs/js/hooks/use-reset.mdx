---
title: useReset()
description: Use the useReset() hook to reset user identity when they log out or when you want to clear the user's identity.
---

## Reset user identity

The `useReset()` hook is used to reset user identity when they log out or when you want to clear the user's identity.

```tsx title="pages/logout.tsx" 
import React from 'react';
// [!code ++:1]
import { useReset } from '@journium/react';

function LogoutButton() {
  // [!code ++:1]
  const reset = useReset();

  const handleLogout = async () => {
    await logoutUser();
    // [!code ++:1]
    reset();
  };

  return <button onClick={handleLogout}>Log Out</button>;
}
```